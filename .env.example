DATABASE_URL="postgresql://remax:remax@localhost:5432/remax_crm?schema=public"
AUTH_SECRET="change-me-to-a-random-secret-at-least-32-chars"
AUTH_URL="http://localhost:3000"

# Railway sets DATABASE_URL automatically when you add PostgreSQL.
# Set AUTH_SECRET to a random string (openssl rand -base64 32).
# Set AUTH_URL to your Railway public URL, e.g. https://remax-crm-production.up.railway.app
