services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: remax
      POSTGRES_PASSWORD: remax
      POSTGRES_DB: remax_crm
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
